{
  "name": "cv-encrypted",
  "version": "1.0.0",
  "description": "Encrypted CV with password-protected contact details",
  "main": "encrypt-inject.js",
  "scripts": {
    "encrypt": "node encrypt-inject.js index.html index-test.html",
    "encrypt:plain": "node encrypt-inject.js index.html index-unencrypted.html --no-encrypt",
    "test": "npm run encrypt && echo 'Test file created: index-test.html'"
  },
  "dependencies": {
    "puppeteer": "^24.15.0"
  }
}
